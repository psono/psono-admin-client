repos:
  - repo: local
    hooks:
      - id: frontend-fix
        name: frontend format and fix (biome)
        entry: node_modules/.bin/biome format --write
        language: system
        files: ^src/.*\.(ts|tsx|js|jsx|json)$
        pass_filenames: true
